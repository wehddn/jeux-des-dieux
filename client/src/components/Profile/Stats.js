import React from "react";

const Stats = ({ userProfile }) => {
  return (
    <section className="card d-flex mt-4" aria-label="Statistics">
    <h2 className="mt-4 profile-title">Statistiques</h2>
    <hr className="p-2 hr-line" />
    </section>
  );
};

export default Stats;
